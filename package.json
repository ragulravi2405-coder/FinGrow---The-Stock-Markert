{
  "name": "fingrow",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client",
    "install:all": "npm install --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.0.2"
  }
}
